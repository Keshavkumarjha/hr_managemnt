{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && gunicorn config.wsgi --log-file - --workers 3 --timeout 120",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
