{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn config.wsgi --log-file - --workers 3 --timeout 120",
    "healthcheckPath": "/healthz/",
    "restartPolicyType": "ON_FAILURE"
  }
}
